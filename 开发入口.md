# 企业AI中台开发入口

此目录是基于原框架建立的**独立、可写开发副本**。执行命令时以本文件所在目录为仓库根目录，不依赖原电脑的盘符或用户名。

## 1. 开工顺序

1. 阅读根目录[AGENTS.md](AGENTS.md)、[开发指南](docs/development-guide.md)与[Harness说明](.harness/README.md)。这些是现有工程规则。
2. 阅读[初始化与迁移说明](docs/ai-platform/00-project-bootstrap.md)，确认当前机器环境。
3. 阅读[AI中台方案入口](docs/ai-platform/README.md)与[任务索引](docs/ai-platform/tasks/README.md)。
4. 完成F01剩余的环境和门禁验证，再按依赖执行F02及后续任务。每次领取一张任务卡，保留验证证据。

## 2. 已整理的内容

- 后端、前端、数据库、docs、scripts、ops及隐藏的.github/.harness等框架文件。
- 102张开发任务卡、需求、架构、API/Chat、升级策略和协议样例，统一在`docs/ai-platform/`。
- 独立Git仓库，开发分支`main`；远程地址以`git remote -v`为准。
- [框架基线清单](docs/framework-baseline.json)记录原框架commit及2,576个文件的SHA-256。
- `framework-baseline-23a7edb37593`标签保存原框架的完整源码快照，供以后比较框架更新；标签与代码一起推送，换电脑后可获取。

## 3. 当前范围

本项目已完成目录、代码基线、文档和独立Git的初始化。AI功能和完整运行验收按任务卡继续执行。

已执行检查及剩余项见[初始化验收摘要](docs/ai-platform/verification/bootstrap-summary.md)。

历史文档包、初始化迁移包及原始源码ZIP已移到项目外的同级`中台-归档/2026-09-16-初始化/`。这些是历史快照，后续维护只修改当前仓库和`docs/ai-platform/`。

## 4. 移到另一台电脑

优先从私有Git仓库克隆，再运行`git fetch --tags`获取框架基线。离线迁移使用`git bundle`，具体命令见[初始化与迁移说明](docs/ai-platform/00-project-bootstrap.md)。本机.env、个人工具配置、缓存、依赖目录和校验日志不进入Git，新电脑独立配置。

新电脑按框架[README](README.md)和[部署说明](docs/deployment.md)安装工具并配置自己的环境变量。Linux环境首次使用执行`chmod +x 后端代码/basic-framework-boot/mvnw`，随后运行`bash scripts/doctor.sh`检查环境。

文档校验与打包脚本使用自身位置或命令参数定位路径。规划中的`E:\kuangjia\2026-main`只记录原始只读来源，不能当作新电脑的开发路径。

## 5. 给后续开发模型的启动提示

```text
本目录是已初始化的企业AI中台开发副本，允许在本目录完成指定开发任务。
先读根AGENTS.md、docs/development-guide.md、.harness/README.md、开发入口.md，
然后读docs/ai-platform/README.md、00-project-bootstrap.md和本次任务卡。
先核验已有初始化结果及依赖任务证据，不重复复制原框架或重建Git仓库。
原始参考框架保持只读；所有本机绝对路径以当前工作目录为准。
F01的完整构建、依赖与集成验证未完成前，不宣称F01 DONE。
只实施本次明确指定的任务，按框架规范验证并报告未验证项。
提交、推送、部署和外部写入需取得相应明确授权。
```
