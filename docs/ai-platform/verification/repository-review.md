# 仓库整理与首次提交复核

日期：2026-09-16。范围：目录合理性、可迁移性、提交边界及现有框架门禁。

## 目录结论

- 保留`后端代码`、`前端代码`、`数据库文件`以及原`.harness`、`.github`、`ops`，这些路径已被构建、CI、文档与任务卡使用。
- AI产品方案统一在`docs/ai-platform`，框架公共规范留在`docs`原位置。
- 历史ZIP和只支持空仓库的打包器移到同级`中台-归档/2026-09-16-初始化`，Git不提交压缩包、个人配置、依赖目录或运行日志。
- 原框架源码保存在`framework-baseline-23a7edb37593`标签；文件模式与Git blob全部对照`docs/framework-baseline.json`验证。后续通过Git获取，不要求新电脑保留原E盘目录。
- `ip2region.xdb`是原框架的运行资源，应保留在所属模块；它不属于交付压缩包。

## 修正

- 根`.gitattributes`统一跨平台文本换行，批处理保留Windows签出格式。
- 秘密扫描首次覆盖完整框架时产生已知测试字面量、UI文字和绑定误报；按原有精确例外方式登记，新增拒绝测试，无整文件或整目录豁免。
- 开发入口、F01任务卡与迁移说明同步到正式Git工作方式，历史初始化报告保留其时点，避免把旧迁移ZIP当作最新版。
- 修正Windows提交钩子无法定位前端Lefthook的问题：使用相对Node入口，补做提交消息校验，并用非法提交消息验证拒绝行为。

## 验证状态

| 检查 | 结果 |
|---|---|
| 原框架源目录 | 只读访问；HEAD及工作树不变 |
| Harness backend | 通过；1,409项测试，0失败、0错误、1跳过 |
| Harness lockfile | 通过；使用项目指定pnpm 10.28.2冻结安装 |
| Harness contracts | 通过；秘密扫描登记变化后已重新执行完整门禁 |
| 秘密检查拒绝测试 | 新测试修复前失败、修复后6项全部通过 |
| Windows提交钩子 | 修复前非法消息未被拒绝；修复后按type/subject规则拒绝，合法消息的相对和绝对路径均通过 |
| 完整暂存区秘密扫描 | 通过；常见私钥、云服务令牌和含凭据URL的补充扫描未发现匹配 |
| Harness frontend | 基线提交后重跑通过；33个工作区类型检查、lint、285个测试文件/1,508项测试、生产构建及单文件覆盖率检查通过 |
| Harness dependencies / integration | 未通过：本机Docker daemon未运行，停止于环境检查 |
| 文档校验 | 32项检查通过，102张任务卡及协议样例完整 |
| GitHub权限 | 个人仓库shady2713/ai-platform，Private，仅账户本人为协作者，无待处理邀请 |

首次冻结安装在非交互终端要求`CI=true`，设置后成功；锁文件与业务源码不因环境安装而更新。构建生成的日志和产物不进入提交。

前端初次检查在空仓库无HEAD时停止，建立独立框架基线提交后按同一入口重新通过。安装生成的4个turbo-run本机路径文件已恢复到原框架字节，避免将开发电脑路径带入提交。

本地Gitleaks二进制下载因网络连接重置未完成，因此不声称本地执行了独立Gitleaks扫描；GitHub Actions原有历史秘密扫描继续保留，远程结果以实际运行状态为准。

本机原始输出位于`.local-state/initialization/review-*.log`，不进入Git。Docker相关失败不得解释为通过，完整F01及产品验收仍需关闭剩余项。当前未验证真实模型、业务系统集成、数据库部署或生产运行。
