# A03 实现应用和主体资源授权

| 项目 | 内容 |
|---|---|
| 状态 | PLANNED：本包只完成规划 |
| 阶段 | V1.0 |
| 需求 | FR-05, FR-18，见[产品需求](../02-product-requirements.md) |
| 依赖 | [A02](A02.md), [F07](F07.md) |
| 风险 | HIGH |
| 估算 | L，约 2–3 人工作日；超过3日先细拆子卡 |

## 1. 任务目标与输入

目标：实现应用和主体资源授权。所有依赖必须有验收证据；不能仅检查类/文件存在。

- [01-framework-baseline.md](../01-framework-baseline.md)
- [09-model-handoff.md](../09-model-handoff.md)
- [03-architecture-integration.md](../03-architecture-integration.md)
- [04-api-chat-integration.md](../04-api-chat-integration.md)
- [05-data-security-contracts.md](../05-data-security-contracts.md)
- [12-critical-implementation-blueprints.md](../12-critical-implementation-blueprints.md)

还需读取授权副本最近的AGENTS.md、已有模块README、领域契约和.harness命令入口。框架最新规则优先，差异写入交接记录。

## 2. 允许修改范围

以下相对路径均相对**另行授权的工作副本**。本次规划不授权修改 `E:\kuangjia\2026-main`。目录表示该领域范围，不代表可以批量重写整棵目录。

- `后端代码/basic-framework-boot/basic-framework-module-ai/src/main/java/com/basicframework/module/ai/service/authorization`
- `后端代码/basic-framework-boot/basic-framework-module-ai/src/main/java/com/basicframework/module/ai/domain/policy`
- `后端代码/basic-framework-boot/basic-framework-module-ai/src/main/java/com/basicframework/module/ai/dal/dataobject/grant`
- `后端代码/basic-framework-boot/basic-framework-module-ai/src/main/java/com/basicframework/module/ai/dal/mysql/grant`
- `后端代码/basic-framework-boot/basic-framework-module-ai/src/main/java/com/basicframework/module/ai/controller/admin/grant`
- `后端代码/basic-framework-boot/basic-framework-server/src/main/resources/db/migration`
- `数据库文件/basic_framework.sql`
- `docs/contracts/field-catalog.yaml`
- `docs/contracts/data-lifecycle.json`
- `docs/contracts/data-permission-exemptions.json`
- `docs/contracts/permission-catalog.json`
- `docs/data-lifecycle.md`
- `docs/security`

补充允许：同一变更对应的src/test、同目录*.test.ts/组件测试、所属模块README、必要的Convert映射，以及新增字段/权限的现有契约台账。这不授权改变其他模块内部实现。修改依赖版本、父POM、全局请求器或Harness拓扑，必须已明确列入本卡，否则先更新任务范围。DB仅允许领取新迁移号和同步最新快照，禁止改历史迁移。

## 3. 逐步实施

先列出本卡实际要创建/修改的文件、对外方法和验收用例，然后按顺序实施：

1. 建立资源grant目录和动作白名单。
2. 实现app/service/subject求交和当前版本检查。
3. 应用与主体撤销更新authz revision，不引入跨请求旧授权缓存。
4. 实现统一authorize(context, action, resource)入口，应用/主体/scope/服务绑定/对象权限缺一不可。
5. 定义结果scope指纹与历史资源再鉴权接口，数据集仍获授权但行范围收窄时不得读取旧聚合。

每一步完成后编译或运行相应focused测试；共享接口先由契约确定，禁止消费者各自猜字段。发现本卡大于3个人工作日时，按持久化/服务/协议/页面/验证拆子卡，保留需求编号和完成证据。

## 4. 验收与失败分支

- AT-009/010/025
- app上限不足拒绝
- 请求资源列表只能缩小范围
- 不同资源同ID不串权

关联验收用例：AT-009, AT-010, AT-025 详见[验收目录](../08-testing-acceptance.md)。本卡未列编号的专项测试同样是完成条件。

## 5. 必须产出

- 授权服务
- 逐表证据
- 管理API与否定用例

## 6. 工程约束

- 不得修改只读源框架；路径均相对另行授权的工作副本。
- 先检查依赖任务的测试证据与公开契约，禁止用临时stub冒充依赖完成。
- 公开契约变更先改权威Schema和样例再改消费者，不能降低校验或覆盖率基线。
- 构造器注入；Controller VO只属于协议层；Service/DAL不反向导入VO；跨模块只用公开CommonApi/DTO。
- 测试使用真实MySQL/Redis Testcontainers或JUnit/Mockito；不得新建H2测试绕过真实约束。
- 只新增未执行迁移，编号领取时查询最新值；同步SQL快照、字段、生命周期、数据权限及菜单权限台账。

## 7. 验证执行

在授权副本根目录执行相关门禁；不得在只读源框架执行。具体focused测试类/脚本由本卡新增的真实测试决定，并在交接记录写出确切命令。

```powershell
& .\.harness\verify.ps1 contracts
& .\.harness\verify.ps1 backend
& .\.harness\verify.ps1 integration
```

阶段结束还要运行完整Harness。浏览器测试命令在Q06建立前不得声称已经存在；跨源/渲染/权限用例在Q06与G5必须用真实浏览器补齐，早期组件测试不能替代最终浏览器验收。环境缺口写未验证，不降低门禁。

## 8. 完成证据

- 变更文件清单和逐项验收结果。
- 实际命令、绝对工作目录、依赖版本、退出码及测试报告路径。
- 新增/变化的API、字段、权限和上游依赖契约差异。
- 未验证项必须说明；关键项未通过不得标记DONE。

交接格式见[模型交接规范](../09-model-handoff.md)。只有实现和本卡适用验证完成才能DONE；文档、Mock、生产实现的证据分开。

## 9. 应停止依赖工作并报告的条件

- 依赖任务未完成或没有可复核证据。
- 需要超出允许路径、引入第二套身份/存储体系或改变已冻结契约。
- 没有用户明确授权的安全实现、生产动作、外部写入或部署。
- 上游候选无法通过兼容、安全或维护状态验证；不能擅自换成不受控远程服务。

停止该依赖链不表示原需求取消；清楚记录阻断条件及可继续的独立任务。不得以TODO、假数据、空实现或注释测试作为交付。
